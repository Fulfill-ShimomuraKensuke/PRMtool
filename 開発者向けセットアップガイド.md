# PRM Tool - 開発者向けセットアップガイド

**対象読者**: 新規参加の開発者  
**最終更新**: 2026-01-16

---

## 📋 前提条件

以下のソフトウェアがインストールされていることを確認してください：

- **Java**: 17以上
- **Node.js**: 18.x以上
- **Docker**: 20.x以上
- **Docker Compose**: 2.x以上
- **Maven**: 3.8以上
- **Git**: 2.x以上

---

## 🚀 クイックスタート（5分）

### 1. リポジトリのクローン

```bash
git clone <repository-url>
cd prmtool
```

### 2. データベースの起動

```bash
docker-compose up -d
```

### 3. Backendの起動

```bash
cd backend
mvn clean install
mvn spring-boot:run
```

### 4. Frontendの起動（別ターミナル）

```bash
cd frontend
npm install
npm start
```

### 5. ブラウザでアクセス

```
http://localhost:3000
```

**初回ログイン情報**:
- ログインID: `system`
- パスワード: `SystemPass123!`

---

## 📦 詳細セットアップ

### Step 1: Java環境の確認

```bash
java -version
# java version "17.0.x" 以上であることを確認
```

Javaがインストールされていない場合:
- **Windows**: [Oracle JDK](https://www.oracle.com/java/technologies/downloads/) または [OpenJDK](https://adoptium.net/)
- **macOS**: `brew install openjdk@17`
- **Linux**: `sudo apt install openjdk-17-jdk`

### Step 2: Node.js環境の確認

```bash
node -v
# v18.x.x 以上であることを確認
```

Node.jsがインストールされていない場合:
- [Node.js公式サイト](https://nodejs.org/)からダウンロード
- または `nvm`（Node Version Manager）を使用

### Step 3: Docker環境の確認

```bash
docker -v
docker-compose -v
```

Dockerがインストールされていない場合:
- **Windows/macOS**: [Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Linux**: 公式ドキュメントに従ってインストール

### Step 4: PostgreSQLの起動

```bash
# プロジェクトルートで実行
docker-compose up -d

# ログの確認
docker logs prmtool-postgres

# 接続確認
docker exec -it prmtool-postgres psql -U prmuser -d prmdb
```

**データベース接続情報**:
- Host: localhost
- Port: 5432
- Database: prmdb
- Username: prmuser
- Password: devpassword123

### Step 5: Backendのビルドと起動

```bash
cd backend

# 依存関係のダウンロードとビルド
mvn clean install

# テストをスキップしてビルド（オプション）
mvn clean install -DskipTests

# アプリケーションの起動
mvn spring-boot:run
```

**起動確認**:
```bash
# ヘルスチェック
curl http://localhost:8080/api/health
```

### Step 6: Frontendのセットアップと起動

```bash
cd frontend

# 依存関係のインストール
npm install

# 開発サーバーの起動
npm start
```

ブラウザが自動的に開かない場合は、`http://localhost:3000`にアクセスしてください。

---

## 🔧 開発環境の設定

### IntelliJ IDEA（推奨）

#### プロジェクトのインポート

1. IntelliJ IDEAを起動
2. `File` → `Open` → `backend` フォルダを選択
3. Mavenプロジェクトとして認識されることを確認
4. 依存関係の自動ダウンロードを待つ

#### 実行構成の設定

1. `Run` → `Edit Configurations`
2. `+` → `Spring Boot`
3. 以下を設定:
   - Name: `PRM Tool Backend`
   - Main class: `com.example.prmtool.PrmtoolApplication`
   - Environment variables: `SPRING_PROFILES_ACTIVE=dev`

### Visual Studio Code

#### 推奨拡張機能

**Backend開発**:
- Extension Pack for Java
- Spring Boot Extension Pack
- Lombok Annotations Support

**Frontend開発**:
- ES7+ React/Redux/React-Native snippets
- ESLint
- Prettier - Code formatter

#### プロジェクトを開く

```bash
code .
```

---

## 🛠️ 開発ワークフロー

### Backendの開発

#### コードの変更

```bash
# 自動再起動（Spring Boot DevTools）
# application.ymlでdevtoolsを有効にしている場合、
# コード変更時に自動的に再起動します
```

#### テストの実行

```bash
cd backend

# 全テストを実行
mvn test

# 特定のテストクラスを実行
mvn test -Dtest=UserServiceTest

# 特定のテストメソッドを実行
mvn test -Dtest=UserServiceTest#testCreateUser
```

#### ログの確認

```bash
# コンソールでログを確認
# または
tail -f logs/application.log
```

### Frontendの開発

#### コードの変更

React開発サーバーはホットリロードに対応しているため、コード変更時に自動的にブラウザがリロードされます。

#### Lintの実行

```bash
cd frontend

# ESLintチェック
npm run lint

# 自動修正
npm run lint:fix
```

#### ビルド

```bash
cd frontend

# 本番ビルド
npm run build

# ビルド結果は build/ ディレクトリに出力
```

---

## 📊 データベース管理

### pgAdmin を使う場合

1. [pgAdmin](https://www.pgadmin.org/)をインストール
2. 新しいサーバーを追加:
   - Name: PRM Tool Dev
   - Host: localhost
   - Port: 5432
   - Username: prmuser
   - Password: devpassword123

### コマンドラインから接続

```bash
# PostgreSQLコンテナに接続
docker exec -it prmtool-postgres psql -U prmuser -d prmdb

# テーブル一覧
\dt

# テーブル構造の確認
\d users

# クエリの実行
SELECT * FROM users;

# 終了
\q
```

### データベースのリセット

```bash
# コンテナを停止・削除
docker-compose down -v

# 再度起動
docker-compose up -d

# Backendを再起動してスキーマを再作成
cd backend
mvn spring-boot:run
```

---

## 🔍 デバッグ

### Backend（IntelliJ IDEA）

1. ブレークポイントを設定
2. デバッグモードで起動（虫のアイコン）
3. ブレークポイントで停止し、変数を確認

### Backend（VS Code）

1. `.vscode/launch.json`を作成:
```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Debug PRM Tool",
      "request": "attach",
      "hostName": "localhost",
      "port": 5005
    }
  ]
}
```

2. デバッグモードで起動:
```bash
mvn spring-boot:run -Dspring-boot.run.jvmArguments="-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=n,address=5005"
```

### Frontend（Chrome DevTools）

1. `F12`を押して開発者ツールを開く
2. `Sources`タブでブレークポイントを設定
3. コードを実行してデバッグ

---

## 🧪 テストデータの投入

### 初回管理者の作成（自動）

アプリケーション起動時に自動的に作成されます。

### テストユーザーの作成

```bash
# ログイン
ログインID: system
パスワード: SystemPass123!

# アカウント管理画面から以下を作成:
1. ADMIN ユーザー
   - 名前: 管理者テスト
   - ログインID: admin01
   - パスワード: Admin123!
   - ロール: 管理者

2. REP ユーザー
   - 名前: 担当者テスト
   - ログインID: rep01
   - パスワード: Rep123!
   - ロール: 担当者
```

### テストパートナーの作成

```bash
# ADMIN ユーザーでログイン

# パートナー管理画面から作成:
1. 株式会社テストA
   - 業種: IT
   - 連絡先名: 田中太郎
   - 連絡先情報: tanaka@test.com

2. 株式会社テストB
   - 業種: 製造
   - 連絡先名: 山田花子
   - 連絡先情報: yamada@test.com
```

---

## 🔧 環境変数の設定

### 開発環境

プロジェクトルートに `.env`ファイルを作成:

```bash
# Database
DB_URL=jdbc:postgresql://localhost:5432/prmdb
DB_USERNAME=prmuser
DB_PASSWORD=devpassword123

# JWT
JWT_SECRET=mySecretKeyForJwtTokenGenerationAndValidationPurpose12345678901234567890
JWT_EXPIRATION=86400000

# Server
SERVER_PORT=8080

# Initial Admin
INITIAL_ADMIN_ENABLED=true
INITIAL_ADMIN_NAME=初期システム管理者
INITIAL_ADMIN_LOGIN_ID=system
INITIAL_ADMIN_EMAIL=system@example.com
INITIAL_ADMIN_PASSWORD=SystemPass123!
```

### プロファイルの切り替え

```bash
# テスト環境で起動
mvn spring-boot:run -Dspring-boot.run.profiles=test

# 本番環境で起動
mvn spring-boot:run -Dspring-boot.run.profiles=prod
```

---

## 📝 コーディング規約

### Backend（Java）

- **スタイルガイド**: Google Java Style Guide
- **命名規則**:
  - クラス: PascalCase（例: `UserService`）
  - メソッド: camelCase（例: `getUserById`）
  - 定数: UPPER_SNAKE_CASE（例: `MAX_RETRY_COUNT`）
- **コメント**: 必須（public メソッドには必ず）

### Frontend（JavaScript）

- **スタイルガイド**: Airbnb JavaScript Style Guide
- **命名規則**:
  - コンポーネント: PascalCase（例: `UserList`）
  - 関数: camelCase（例: `handleSubmit`）
  - 定数: UPPER_SNAKE_CASE（例: `API_BASE_URL`）
- **コメント**: 複雑なロジックには必須

---

## 🚨 トラブルシューティング

### ポートが使用中

```bash
# ポート8080を使用しているプロセスを確認
lsof -i :8080  # macOS/Linux
netstat -ano | findstr :8080  # Windows

# プロセスを停止するか、別のポートを使用
SERVER_PORT=8081 mvn spring-boot:run
```

### Mavenのビルドエラー

```bash
# キャッシュをクリア
mvn clean install -U

# .m2/repositoryを削除
rm -rf ~/.m2/repository
mvn clean install
```

### npm installのエラー

```bash
# node_modulesを削除して再インストール
rm -rf node_modules package-lock.json
npm install

# またはnpmキャッシュをクリア
npm cache clean --force
npm install
```

### データベース接続エラー

```bash
# PostgreSQLコンテナが起動しているか確認
docker ps

# 起動していない場合
docker-compose up -d

# ログを確認
docker logs prmtool-postgres
```

---

## 📚 参考リソース

### 公式ドキュメント
- [Spring Boot](https://spring.io/projects/spring-boot)
- [React](https://react.dev/)
- [PostgreSQL](https://www.postgresql.org/docs/)

### チュートリアル
- [Spring Boot Tutorial](https://spring.io/guides)
- [React Tutorial](https://react.dev/learn)

---

**質問やサポートが必要な場合は、チームリーダーに連絡してください。**

Happy Coding! 🎉
