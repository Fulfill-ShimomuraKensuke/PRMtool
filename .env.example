# Spring Boot プロファイル（dev / prod）
SPRING_PROFILES_ACTIVE=dev

# サーバー設定
SERVER_PORT=8080

# データベース設定（本番環境用）
DB_URL=jdbc:postgresql://localhost:5432/prmdb
DB_USERNAME=prmuser
DB_PASSWORD=your_secure_password_here

# JWT設定
# 本番環境では必ず256bit以上のランダム文字列に変更すること
# 生成方法: openssl rand -base64 64
JWT_SECRET=mySecretKeyForJwtTokenGenerationAndValidationPurpose12345678901234567890
JWT_EXPIRATION=86400000

# CORS設定（本番環境）
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com