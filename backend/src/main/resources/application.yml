# ==========================================
# PRM Tool - Application Configuration
# ==========================================

spring:
  application:
    name: prmtool
  
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

# Bootstrap用の管理者キー
app:
  initial-admin-key: ${INITIAL_ADMIN_KEY:dev-bootstrap-secret}
  
  # 初期システム管理者の自動作成設定（SYSTEMロール）
  initial-admin:
    enabled: ${INITIAL_ADMIN_ENABLED:true}
    name: ${INITIAL_ADMIN_NAME:初期システム管理者}
    loginId: ${INITIAL_ADMIN_LOGIN_ID:system}
    email: ${INITIAL_ADMIN_EMAIL:system@example.com}
    password: ${INITIAL_ADMIN_PASSWORD:SystemPass123!}

---
# ==========================================
# 開発環境設定（PostgreSQL使用）
# ==========================================
spring:
  config:
    activate:
      on-profile: dev
      
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/prmdb}
    username: ${DB_USERNAME:prmuser}
    password: ${DB_PASSWORD:devpassword123}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      # Flywayに任せるためnoneに設定
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        highlight_sql: true

  # ==========================================
  # Flyway設定（データベースマイグレーション）
  # ==========================================
  flyway:
    # Flywayを有効化
    enabled: true
    # マイグレーションファイルの配置場所
    locations: classpath:db/migration
    # ファイルのエンコーディング
    encoding: UTF-8
    # 既存のデータベースに対してFlywayを初めて適用する場合
    baseline-on-migrate: true
    # ベースラインバージョン
    baseline-version: 0
    # マイグレーション実行前に検証
    validate-on-migrate: true
    # 順序が前後したマイグレーションを許可しない
    out-of-order: false
    # プレースホルダーの使用
    placeholder-replacement: true

  # ==========================================
  # メール送信設定（開発環境: Xサーバー）
  # ==========================================
  mail:
    host: sv8915.xserver.jp
    port: 465
    username: shimomura-kensuke@fulfill-group.co.jp
    # パスワードは直接記載（.envから読めない問題の回避）
    password: simomura3109
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  # ファイルアップロード設定
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

---
# ==========================================
# 本番環境設定（PostgreSQL使用）
# ==========================================
spring:
  config:
    activate:
      on-profile: prod
      
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/prmdb}
    username: ${DB_USERNAME:prmuser}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
    
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      # 本番環境ではvalidateのまま
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true

  # ==========================================
  # Flyway設定（本番環境）
  # ==========================================
  flyway:
    enabled: true
    locations: classpath:db/migration
    encoding: UTF-8
    baseline-on-migrate: true
    baseline-version: 0
    validate-on-migrate: true
    out-of-order: false

  # ==========================================
  # メール送信設定（本番環境: Xサーバー）
  # ==========================================
  mail:
    host: sv8915.xserver.jp
    port: 465
    username: shimomura-kensuke@fulfill-group.co.jp
    password: simomura3109
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

---
# ==========================================
# ローカルテスト環境設定（H2使用）
# ==========================================
spring:
  config:
    activate:
      on-profile: test
      
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
    
  h2:
    console:
      enabled: true
      path: /h2-console
      
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      # テスト環境では毎回作り直し
      ddl-auto: create-drop
    show-sql: true

  # ==========================================
  # Flyway設定（テスト環境は無効化）
  # ==========================================
  flyway:
    # テスト環境ではFlywayを無効化（create-dropを使用）
    enabled: false

  # ==========================================
  # メール送信設定（テスト環境: 無効化）
  # ==========================================
  mail:
    host: localhost
    port: 1025
    username: test
    password: test

---
# ==========================================
# 共通設定（全環境）
# ==========================================

# JWT設定
jwt:
  secret: ${JWT_SECRET:mySecretKeyForJwtTokenGenerationAndValidationPurpose12345678901234567890}
  expiration: ${JWT_EXPIRATION:86400000}

# サーバー設定
server:
  port: ${SERVER_PORT:8080}
  compression:
    enabled: true
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param

# ログ設定
logging:
  level:
    root: INFO
    com.example.prmtool: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    # Flywayのログも表示
    org.flywaydb: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# ========================================
# メール送信設定（共通）
# ========================================
mail:
  from:
    # GmailのSMTP認証ユーザーと必ず同じアドレスを指定する
    email: ${MAIL_FROM_EMAIL:shimomura-kensuke@fulfill-group.co.jp}
    name: ${MAIL_FROM_NAME:PRM Tool}

# ========================================
# ファイルストレージ設定（現在: ローカル）
# ========================================
file:
  storage:
    type: local
    local:
      upload-dir: ./uploads
    max-file-size: 10MB
    allowed-extensions: pdf,doc,docx,xls,xlsx,ppt,pptx,txt,png,jpg,jpeg,gif

# ========================================
# S3移行時の設定例（現在はコメントアウト）
# ========================================
# file:
#   storage:
#     type: s3
#     s3:
#       bucket-name: your-bucket-name
#       region: ap-northeast-1
#       access-key: ${AWS_ACCESS_KEY}
#       secret-key: ${AWS_SECRET_KEY}
#     max-file-size: 10MB
#     allowed-extensions: pdf,doc,docx,xls,xlsx,ppt,pptx,txt,png,jpg,jpeg,gif