# PRM Tool 開発者向けセットアップガイド

**バージョン:** 1.0  
**最終更新日:** 2026年2月10日

---

## 📚 目次

1. [はじめに](#はじめに)
2. [システム要件](#システム要件)
3. [開発環境のセットアップ](#開発環境のセットアップ)
4. [データベースセットアップ](#データベースセットアップ)
5. [バックエンド開発](#バックエンド開発)
6. [フロントエンド開発](#フロントエンド開発)
7. [コーディング規約](#コーディング規約)
8. [テスト](#テスト)
9. [デプロイ](#デプロイ)
10. [トラブルシューティング](#トラブルシューティング)

---

## はじめに

このガイドは、PRM Toolの開発環境をセットアップし、開発を開始するための手順を説明します。

### 対象読者

- PRM Toolの開発に参加する開発者
- システムの拡張やカスタマイズを行う技術者

### 前提知識

- Java（Spring Boot）の基礎知識
- React（JavaScript/TypeScript）の基礎知識
- PostgreSQLの基礎知識
- Git の基礎知識

---

## システム要件

### 必須ソフトウェア

| ソフトウェア | バージョン | ダウンロード |
|------------|-----------|------------|
| JDK | 17以上 | https://adoptium.net/ |
| Node.js | 18.x以上 | https://nodejs.org/ |
| PostgreSQL | 16以上 | https://www.postgresql.org/download/ |
| Maven | 3.8.x以上 | https://maven.apache.org/download.cgi |
| Git | 最新版 | https://git-scm.com/downloads |

### 推奨IDE

| IDE | 用途 | ダウンロード |
|-----|------|------------|
| IntelliJ IDEA | バックエンド開発 | https://www.jetbrains.com/idea/ |
| Visual Studio Code | フロントエンド開発 | https://code.visualstudio.com/ |

### 推奨VS Code拡張機能

- **ESLint** - JavaScript/Reactのlinter
- **Prettier** - コードフォーマッター
- **GitLens** - Git拡張機能
- **REST Client** - APIテスト

---

## 開発環境のセットアップ

### 1. JDK のインストール

#### Windows
```powershell
# Chocolatey を使用
choco install openjdk17

# 環境変数確認
java -version
```

#### macOS
```bash
# Homebrew を使用
brew install openjdk@17

# パスを通す
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# 確認
java -version
```

#### Linux
```bash
# apt を使用（Ubuntu/Debian）
sudo apt update
sudo apt install openjdk-17-jdk

# 確認
java -version
```

---

### 2. Node.js のインストール

#### Windows
```powershell
# Chocolatey を使用
choco install nodejs-lts

# 確認
node -v
npm -v
```

#### macOS
```bash
# Homebrew を使用
brew install node@18

# 確認
node -v
npm -v
```

#### Linux
```bash
# nvm を使用（推奨）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc

nvm install 18
nvm use 18

# 確認
node -v
npm -v
```

---

### 3. PostgreSQL のインストール

#### Windows
```powershell
# Chocolatey を使用
choco install postgresql16

# サービス起動
net start postgresql-x64-16
```

#### macOS
```bash
# Homebrew を使用
brew install postgresql@16

# サービス起動
brew services start postgresql@16
```

#### Linux
```bash
# apt を使用（Ubuntu/Debian）
sudo apt update
sudo apt install postgresql-16

# サービス起動
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

---

### 4. Maven のインストール

#### Windows
```powershell
# Chocolatey を使用
choco install maven

# 確認
mvn -version
```

#### macOS
```bash
# Homebrew を使用
brew install maven

# 確認
mvn -version
```

#### Linux
```bash
# apt を使用（Ubuntu/Debian）
sudo apt install maven

# 確認
mvn -version
```

---

### 5. リポジトリのクローン

```bash
# GitHubからクローン
git clone https://github.com/your-org/prm-tool.git
cd prm-tool

# ブランチ確認
git branch -a

# 開発ブランチに切り替え（例）
git checkout develop
```

---

## データベースセットアップ

### 1. PostgreSQL にログイン

```bash
# macOS/Linux
psql -U postgres

# Windows
psql -U postgres -W
```

### 2. データベース作成

```sql
-- データベース作成
CREATE DATABASE prmtool;

-- 開発用ユーザー作成（任意）
CREATE USER prmdev WITH PASSWORD 'prmdev123';

-- 権限付与
GRANT ALL PRIVILEGES ON DATABASE prmtool TO prmdev;

-- テスト用データベース作成（任意）
CREATE DATABASE prmtool_test;
GRANT ALL PRIVILEGES ON DATABASE prmtool_test TO prmdev;

-- 終了
\q
```

### 3. データベース接続確認

```bash
psql -U prmdev -d prmtool
```

**成功したら:**
```
prmtool=>
```

---

## バックエンド開発

### 1. プロジェクト構造

```
backend/
├── src/
│   ├── main/
│   │   ├── java/com/example/prmtool/
│   │   │   ├── config/          # 設定クラス
│   │   │   │   ├── CorsConfig.java
│   │   │   │   └── SecurityConfig.java
│   │   │   ├── controller/      # RESTコントローラー
│   │   │   │   ├── AuthController.java
│   │   │   │   ├── UserController.java
│   │   │   │   ├── PartnerController.java
│   │   │   │   ├── ProjectController.java
│   │   │   │   ├── CommissionRuleController.java
│   │   │   │   ├── InvoiceController.java
│   │   │   │   └── ContentManagementController.java
│   │   │   ├── dto/             # データ転送オブジェクト
│   │   │   ├── entity/          # エンティティ
│   │   │   ├── repository/      # リポジトリ
│   │   │   ├── security/        # セキュリティ関連
│   │   │   │   ├── JwtTokenProvider.java
│   │   │   │   └── JwtAuthenticationFilter.java
│   │   │   └── service/         # サービスクラス
│   │   └── resources/
│   │       ├── application.properties
│   │       └── db/migration/    # Flywayマイグレーション
│   │           ├── V1__Create_users_table.sql
│   │           ├── V2__Create_partners_table.sql
│   │           └── ...
│   └── test/                    # テストコード
└── pom.xml                      # Maven設定
```

### 2. application.properties の設定

**開発環境用:**
```properties
# サーバー設定
server.port=8080

# データベース接続
spring.datasource.url=jdbc:postgresql://localhost:5432/prmtool
spring.datasource.username=prmdev
spring.datasource.password=prmdev123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA設定
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway設定
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# JWT設定
jwt.secret=your-very-long-secret-key-at-least-256-bits-for-HS256-algorithm
jwt.expiration=86400000

# ファイルアップロード設定
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.enabled=true

# ファイルストレージパス（開発環境）
file.storage.path=./storage

# ログ設定
logging.level.com.example.prmtool=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
```

### 3. ビルドと起動

```bash
cd backend

# 依存関係のダウンロードとビルド
mvn clean install

# 起動
mvn spring-boot:run

# ポート確認
# http://localhost:8080 にアクセス
```

**起動成功のログ例:**
```
Started PrmtoolApplication in 5.123 seconds
```

### 4. データベースマイグレーション

#### 新しいマイグレーションファイルの作成

```bash
cd backend/src/main/resources/db/migration

# ファイル作成（例）
touch V10__Add_new_feature.sql
```

**ファイル名規則:**
- `V{バージョン番号}__{説明}.sql`
- バージョン番号は連番（V1, V2, V3...）
- 説明はアンダースコアで区切る

**マイグレーションファイルの例:**
```sql
-- V10__Add_new_feature.sql

-- 新しいテーブル作成
CREATE TABLE new_table (
    id UUID PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- インデックス作成
CREATE INDEX idx_new_table_name ON new_table(name);
```

#### マイグレーション実行

```bash
# アプリケーションを起動すると自動的に実行される
mvn spring-boot:run
```

#### マイグレーション履歴確認

```sql
-- PostgreSQLにログイン
psql -U prmdev -d prmtool

-- Flywayの履歴テーブルを確認
SELECT * FROM flyway_schema_history ORDER BY installed_rank;
```

---

## フロントエンド開発

### 1. プロジェクト構造

```
frontend/
├── public/                      # 静的ファイル
│   ├── index.html
│   └── favicon.ico
├── src/
│   ├── components/              # 共通コンポーネント
│   ├── pages/                   # ページコンポーネント
│   │   ├── Login.js
│   │   ├── Dashboard.js
│   │   ├── Users.js
│   │   ├── Partners.js
│   │   ├── Projects.js
│   │   ├── CommissionRules.js
│   │   ├── Invoices.js
│   │   ├── Contents.js
│   │   └── ContentShares.js
│   ├── services/                # APIサービス
│   │   ├── authService.js
│   │   ├── userService.js
│   │   ├── partnerService.js
│   │   ├── projectService.js
│   │   ├── commissionRuleService.js
│   │   ├── invoiceService.js
│   │   ├── contentService.js
│   │   └── contentShareService.js
│   ├── App.js                   # メインアプリ
│   ├── App.css                  # グローバルスタイル
│   └── index.js                 # エントリーポイント
├── .env                         # 環境変数
└── package.json                 # npm設定
```

### 2. 環境変数の設定

**`.env` ファイルを作成:**
```bash
cd frontend
touch .env
```

**`.env` の内容:**
```env
# バックエンドAPIのURL
REACT_APP_API_URL=http://localhost:8080

# その他の環境変数（必要に応じて追加）
REACT_APP_ENV=development
```

### 3. 依存関係のインストールと起動

```bash
cd frontend

# 依存関係インストール
npm install

# 開発サーバー起動
npm start

# ブラウザが自動的に開きます
# http://localhost:3000
```

**起動成功のログ例:**
```
Compiled successfully!

You can now view prm-tool in the browser.

  Local:            http://localhost:3000
  On Your Network:  http://192.168.1.100:3000
```

### 4. APIサービスの作成パターン

**例: userService.js**
```javascript
import api from './api';

// ユーザー一覧取得
export const getUsers = async () => {
  const response = await api.get('/api/users');
  return response.data;
};

// ユーザー作成
export const createUser = async (userData) => {
  const response = await api.post('/api/users', userData);
  return response.data;
};

// ユーザー更新
export const updateUser = async (id, userData) => {
  const response = await api.put(`/api/users/${id}`, userData);
  return response.data;
};

// ユーザー削除
export const deleteUser = async (id) => {
  const response = await api.delete(`/api/users/${id}`);
  return response.data;
};

export default {
  getUsers,
  createUser,
  updateUser,
  deleteUser,
};
```

### 5. Reactコンポーネントの作成パターン

**例: Users.js**
```javascript
import React, { useState, useEffect } from 'react';
import userService from '../services/userService';
import './Users.css';

const Users = () => {
  // ユーザー一覧を管理するstate
  const [users, setUsers] = useState([]);
  
  // ローディング状態
  const [loading, setLoading] = useState(false);
  
  // エラーメッセージ
  const [error, setError] = useState('');

  // コンポーネントマウント時にユーザー一覧を取得
  useEffect(() => {
    fetchUsers();
  }, []);

  // ユーザー一覧取得
  const fetchUsers = async () => {
    try {
      setLoading(true);
      const data = await userService.getUsers();
      setUsers(data);
      setError('');
    } catch (err) {
      setError('ユーザーの取得に失敗しました');
      console.error(err);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="users-container">
      <h1>ユーザー管理</h1>
      
      {error && <div className="error-message">{error}</div>}
      {loading && <div className="loading">読み込み中...</div>}
      
      <div className="users-list">
        {users.map((user) => (
          <div key={user.id} className="user-card">
            <h3>{user.name}</h3>
            <p>ロール: {user.role}</p>
          </div>
        ))}
      </div>
    </div>
  );
};

export default Users;
```

---

## コーディング規約

### Java（バックエンド）

#### 命名規則

| 種類 | 規則 | 例 |
|------|------|-----|
| クラス | パスカルケース | `UserController`, `PartnerService` |
| メソッド | キャメルケース | `getUsers()`, `createPartner()` |
| 変数 | キャメルケース | `userId`, `partnerName` |
| 定数 | スネークケース（大文字） | `MAX_FILE_SIZE`, `DEFAULT_PAGE_SIZE` |
| パッケージ | 小文字 | `com.example.prmtool.controller` |

#### コメント

**クラスのコメント:**
```java
/**
 * ユーザー管理コントローラー
 * ユーザーのCRUD操作を提供
 */
@RestController
@RequestMapping("/api/users")
public class UserController {
    // ...
}
```

**メソッドのコメント:**
```java
/**
 * ユーザー一覧を取得
 * 
 * @return ユーザー一覧
 */
@GetMapping
public List<UserResponse> getUsers() {
    // ...
}
```

**複雑なロジックのコメント:**
```java
// 手数料タイプに応じて計算方法を変更
if (rule.getCommissionType() == CommissionType.FIXED_AMOUNT) {
    // 固定金額の場合
    commission = rule.getValue();
} else {
    // 割合の場合
    commission = subtotal.multiply(rule.getValue()).divide(BigDecimal.valueOf(100));
}
```

#### インデント

- **スペース4つ**を使用
- タブは使用しない

#### Lombok の使用

**積極的に使用するアノテーション:**
- `@Data` - getter/setter/toString/equals/hashCode
- `@Builder` - ビルダーパターン
- `@NoArgsConstructor` - デフォルトコンストラクタ
- `@AllArgsConstructor` - 全フィールドコンストラクタ

**例:**
```java
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
@Entity
@Table(name = "users")
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.UUID)
    private UUID id;
    
    private String name;
    private String loginId;
    private String passwordHash;
}
```

---

### JavaScript/React（フロントエンド）

#### 命名規則

| 種類 | 規則 | 例 |
|------|------|-----|
| コンポーネント | パスカルケース | `Users`, `PartnerList` |
| 関数 | キャメルケース | `getUsers()`, `handleClick()` |
| 変数 | キャメルケース | `userId`, `partnerName` |
| 定数 | スネークケース（大文字） | `MAX_FILE_SIZE`, `API_URL` |
| ファイル | パスカルケース（コンポーネント） | `Users.js`, `PartnerList.js` |
| ファイル | キャメルケース（サービス） | `userService.js`, `authService.js` |

#### コメント

**コンポーネントのコメント:**
```javascript
/**
 * ユーザー管理画面
 * ユーザーの一覧表示と作成・編集・削除を提供
 */
const Users = () => {
    // ...
};
```

**関数のコメント:**
```javascript
/**
 * ユーザー一覧を取得
 */
const fetchUsers = async () => {
    // ...
};
```

#### インデント

- **スペース2つ**を使用
- タブは使用しない

#### 関数コンポーネントの使用

**クラスコンポーネントは使用せず、関数コンポーネントを使用:**
```javascript
// ❌ クラスコンポーネント（使用しない）
class Users extends React.Component {
  render() {
    return <div>Users</div>;
  }
}

// ✅ 関数コンポーネント（使用する）
const Users = () => {
  return <div>Users</div>;
};
```

#### Hooks の使用

**useState, useEffect を積極的に使用:**
```javascript
const Users = () => {
  // state管理
  const [users, setUsers] = useState([]);
  const [loading, setLoading] = useState(false);
  
  // 副作用
  useEffect(() => {
    fetchUsers();
  }, []);
  
  const fetchUsers = async () => {
    // ...
  };
  
  return <div>...</div>;
};
```

---

## テスト

### バックエンドテスト

#### ユニットテスト

**JUnit 5 を使用:**
```java
@SpringBootTest
class UserServiceTest {
    
    @Autowired
    private UserService userService;
    
    @Test
    void testGetUsers() {
        // テストコード
        List<User> users = userService.getUsers();
        assertNotNull(users);
    }
}
```

#### テスト実行

```bash
cd backend

# 全テスト実行
mvn test

# 特定のテストクラスのみ実行
mvn test -Dtest=UserServiceTest

# テストをスキップしてビルド
mvn clean install -DskipTests
```

---

### フロントエンドテスト

#### React Testing Library を使用

**例: Users.test.js**
```javascript
import { render, screen } from '@testing-library/react';
import Users from './Users';

test('renders users heading', () => {
  render(<Users />);
  const heading = screen.getByText(/ユーザー管理/i);
  expect(heading).toBeInTheDocument();
});
```

#### テスト実行

```bash
cd frontend

# 全テスト実行
npm test

# カバレッジ表示
npm test -- --coverage
```

---

## デプロイ

### Render でのデプロイ

#### バックエンドのデプロイ

1. **Renderダッシュボードにログイン**
   - https://dashboard.render.com/

2. **新しい Web Service を作成**
   - 「New +」 → 「Web Service」
   - GitHubリポジトリを接続

3. **設定**
   ```
   Name: prm-tool-backend
   Environment: Docker / Web Service
   Branch: main
   Build Command: mvn clean install -DskipTests
   Start Command: java -jar target/prmtool-0.0.1-SNAPSHOT.jar
   ```

4. **環境変数を設定**
   ```
   DATABASE_URL=postgresql://...
   JWT_SECRET=your-secret-key
   FILE_STORAGE_PATH=/opt/render/project/src/files
   ```

5. **デプロイ**
   - 「Create Web Service」をクリック

#### フロントエンドのデプロイ

1. **新しい Static Site を作成**
   - 「New +」 → 「Static Site」
   - GitHubリポジトリを接続

2. **設定**
   ```
   Name: prm-tool-frontend
   Branch: main
   Build Command: cd frontend && npm install && npm run build
   Publish Directory: frontend/build
   ```

3. **環境変数を設定**
   ```
   REACT_APP_API_URL=https://prm-tool-backend.onrender.com
   ```

4. **デプロイ**
   - 「Create Static Site」をクリック

---

## トラブルシューティング

### バックエンド

#### 問題: データベースに接続できない

**エラーメッセージ:**
```
org.postgresql.util.PSQLException: Connection refused
```

**解決策:**
1. PostgreSQLが起動しているか確認
   ```bash
   # macOS/Linux
   sudo systemctl status postgresql
   
   # Windows
   net start postgresql-x64-16
   ```

2. `application.properties` の接続情報を確認
   ```properties
   spring.datasource.url=jdbc:postgresql://localhost:5432/prmtool
   spring.datasource.username=prmdev
   spring.datasource.password=prmdev123
   ```

3. データベースが存在するか確認
   ```bash
   psql -U prmdev -d prmtool
   ```

---

#### 問題: Flywayマイグレーションが失敗する

**エラーメッセージ:**
```
FlywayException: Validate failed: Migration checksum mismatch
```

**解決策:**
1. Flywayの履歴をクリア（**開発環境のみ**）
   ```sql
   -- PostgreSQLにログイン
   psql -U prmdev -d prmtool
   
   -- Flywayテーブルを削除
   DROP TABLE flyway_schema_history;
   
   -- アプリケーションを再起動
   ```

2. または、マイグレーションファイルを修正せず、新しいマイグレーションファイルを作成

---

### フロントエンド

#### 問題: バックエンドに接続できない

**エラーメッセージ:**
```
Network Error
```

**解決策:**
1. バックエンドが起動しているか確認
   ```bash
   curl http://localhost:8080
   ```

2. `.env` ファイルを確認
   ```env
   REACT_APP_API_URL=http://localhost:8080
   ```

3. CORSの設定を確認
   ```java
   // CorsConfig.java
   @Override
   public void addCorsMappings(CorsRegistry registry) {
       registry.addMapping("/**")
               .allowedOrigins("http://localhost:3000")
               .allowedMethods("*")
               .allowedHeaders("*")
               .allowCredentials(true);
   }
   ```

---

#### 問題: npm install が失敗する

**エラーメッセージ:**
```
npm ERR! code EACCES
```

**解決策:**
1. Node.jsのバージョンを確認
   ```bash
   node -v
   # v18.x.x 以上であることを確認
   ```

2. `node_modules` と `package-lock.json` を削除して再インストール
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. 権限エラーの場合、npm の設定を変更
   ```bash
   npm config set prefix ~/.npm-global
   ```

---

## お問い合わせ

技術的な質問や問題がある場合は、以下までご連絡ください：

- **Email:** dev-support@example.com
- **Slack:** #prm-tool-dev
- **GitHub Issues:** https://github.com/your-org/prm-tool/issues

---

**ドキュメント終わり**