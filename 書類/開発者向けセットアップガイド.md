# PRM Tool - 開発者向けセットアップガイド

**対象者**: 開発者・エンジニア  
**最終更新**: 2026-01-20  
**難易度**: 中級

---

## 📋 目次

1. [前提条件](#1-前提条件)
2. [環境構築](#2-環境構築)
3. [プロジェクトのセットアップ](#3-プロジェクトのセットアップ)
4. [開発ワークフロー](#4-開発ワークフロー)
5. [コーディング規約](#5-コーディング規約)
6. [デバッグ方法](#6-デバッグ方法)
7. [テスト](#7-テスト)
8. [トラブルシューティング](#8-トラブルシューティング)
9. [よくある質問](#9-よくある質問)

---

## 1. 前提条件

### 1.1 必須ソフトウェア

以下のソフトウェアを事前にインストールしてください。

#### Java Development Kit (JDK)

```bash
# バージョン確認
java -version

# 必要: Java 17以上
# 推奨: OpenJDK 17 または Oracle JDK 17
```

**インストール方法**:

**Windows**:
1. [Adoptium](https://adoptium.net/) から OpenJDK 17 をダウンロード
2. インストーラーを実行
3. 環境変数 `JAVA_HOME` を設定
4. `Path` に `%JAVA_HOME%\bin` を追加

**macOS**:
```bash
# Homebrewを使用
brew install openjdk@17

# パスを設定
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

**Linux (Ubuntu)**:
```bash
sudo apt update
sudo apt install openjdk-17-jdk
```

---

#### Maven

```bash
# バージョン確認
mvn -version

# 必要: Maven 3.8以上
```

**インストール方法**:

**Windows**:
1. [Apache Maven](https://maven.apache.org/download.cgi) からダウンロード
2. ZIP を解凍
3. 環境変数 `MAVEN_HOME` を設定
4. `Path` に `%MAVEN_HOME%\bin` を追加

**macOS**:
```bash
brew install maven
```

**Linux (Ubuntu)**:
```bash
sudo apt install maven
```

---

#### Node.js & npm

```bash
# バージョン確認
node -v
npm -v

# 必要: Node.js 18.x以上、npm 9.x以上
```

**インストール方法**:

**すべてのOS**:
1. [Node.js公式サイト](https://nodejs.org/) からLTS版をダウンロード
2. インストーラーを実行（npmも同時にインストールされます）

**または nvm を使用（推奨）**:

**macOS/Linux**:
```bash
# nvmのインストール
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Node.js 18のインストール
nvm install 18
nvm use 18
```

**Windows**:
[nvm-windows](https://github.com/coreybutler/nvm-windows) を使用

---

#### Docker & Docker Compose

```bash
# バージョン確認
docker -v
docker-compose -v

# 必要: Docker 20.x以上、Docker Compose 2.x以上
```

**インストール方法**:

**Windows/macOS**:
1. [Docker Desktop](https://www.docker.com/products/docker-desktop) をダウンロード
2. インストーラーを実行
3. Docker Desktop を起動

**Linux (Ubuntu)**:
```bash
# Dockerのインストール
sudo apt update
sudo apt install docker.io
sudo systemctl start docker
sudo systemctl enable docker

# Docker Composeのインストール
sudo apt install docker-compose-v2

# 現在のユーザーをdockerグループに追加（sudoなしで実行可能に）
sudo usermod -aG docker $USER
```

---

#### Git

```bash
# バージョン確認
git --version

# 必要: Git 2.x以上
```

**インストール方法**:

**Windows**:
[Git for Windows](https://git-scm.com/download/win) をダウンロードしてインストール

**macOS**:
```bash
brew install git
```

**Linux (Ubuntu)**:
```bash
sudo apt install git
```

**Git初期設定**:
```bash
# ユーザー名とメールアドレスを設定
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# エディタを設定（VS Codeを使用する場合）
git config --global core.editor "code --wait"
```

---

### 1.2 推奨ソフトウェア

#### IDE / エディタ

**IntelliJ IDEA** (Java開発に最適):
- [IntelliJ IDEA Community Edition](https://www.jetbrains.com/idea/download/) (無料)
- または Ultimate Edition (有料、Spring Boot対応が充実)

**VS Code** (軽量でマルチ言語対応):
- [Visual Studio Code](https://code.visualstudio.com/)
- 推奨拡張機能:
  - Java Extension Pack
  - Spring Boot Extension Pack
  - ES7+ React/Redux/React-Native snippets
  - Prettier - Code formatter
  - ESLint

---

#### データベース管理ツール

**pgAdmin** (PostgreSQL GUI):
- [pgAdmin](https://www.pgadmin.org/download/) をダウンロードしてインストール

**DBeaver** (汎用的なDB管理ツール):
- [DBeaver Community](https://dbeaver.io/download/)

---

#### API テストツール

**Postman**:
- [Postman](https://www.postman.com/downloads/) をダウンロード

**または curl** (コマンドライン):
```bash
# 多くのOSにプリインストールされている
curl --version
```

---

## 2. 環境構築

### 2.1 プロジェクトのクローン

```bash
# GitHubからクローン
git clone https://github.com/Fulfill-ShimomuraKensuke/PRMtool.git

# プロジェクトディレクトリに移動
cd PRMtool
```

---

### 2.2 データベースのセットアップ

#### Docker Composeを使用（推奨）

```bash
# PostgreSQLコンテナを起動
docker-compose up -d

# 起動確認
docker ps

# ログ確認
docker logs prmtool-postgres

# データベース接続確認
docker exec -it prmtool-postgres psql -U prmuser -d prmdb
```

**接続情報**:
- ホスト: `localhost`
- ポート: `5432`
- データベース名: `prmdb`
- ユーザー名: `prmuser`
- パスワード: `devpassword123`

#### pgAdminから接続

1. pgAdmin を起動
2. 新しいサーバーを登録
   - 名前: `PRM Tool Local`
   - Host: `localhost`
   - Port: `5432`
   - Database: `prmdb`
   - Username: `prmuser`
   - Password: `devpassword123`

---

### 2.3 Backend のセットアップ

```bash
# backendディレクトリに移動
cd backend

# Mavenの依存関係をインストール
mvn clean install

# アプリケーションの起動
mvn spring-boot:run
```

**起動確認**:
```bash
# 別のターミナルで実行
curl http://localhost:8080/api/health

# 正常な場合、200 OKが返る
```

**初回起動時の動作**:
- データベーステーブルが自動作成される（Hibernate DDL auto）
- 初期管理者（SYSTEM）が自動作成される
  - ログインID: `system`
  - パスワード: `SystemPass123!`

---

### 2.4 Frontend のセットアップ

```bash
# 別のターミナルを開く
cd PRMtool/frontend

# npm依存関係をインストール
npm install

# 開発サーバーを起動
npm start
```

**起動確認**:
- ブラウザが自動的に開き、`http://localhost:3000` が表示される
- ログイン画面が表示されれば成功

**環境変数の確認**:
```bash
# frontend/.env.development を確認
cat .env.development

# 以下が設定されているはず
REACT_APP_API_BASE_URL=http://localhost:8080
```

---

### 2.5 動作確認

#### 1. ログインテスト

1. ブラウザで `http://localhost:3000` にアクセス
2. 以下でログイン:
   - ログインID: `system`
   - パスワード: `SystemPass123!`
3. ダッシュボードが表示されれば成功

#### 2. ADMIN ユーザー作成

1. 「アカウント管理」に移動
2. 「新規ユーザー」をクリック
3. 以下を入力:
   - 名前: `管理者テスト`
   - ログインID: `admin`
   - パスワード: `Admin123!`
   - パスワード確認: `Admin123!`
   - メールアドレス: `admin@example.com`
   - ロール: `管理者`
4. 保存

#### 3. パートナー・案件作成テスト

1. ログアウトして `admin` でログイン
2. 「パートナー管理」からテストパートナーを作成
3. 「案件管理」からテスト案件を作成
4. 案件詳細画面でスプレッドシートが動作するか確認

---

## 3. プロジェクトのセットアップ

### 3.1 プロジェクト構成の理解

```
PRMtool/
├── backend/              # Spring Boot アプリケーション
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/example/prmtool/
│   │   │   │   ├── config/       # 設定クラス
│   │   │   │   │   ├── CorsConfig.java
│   │   │   │   │   ├── DataInitializer.java
│   │   │   │   │   ├── JwtAuthenticationFilter.java
│   │   │   │   │   ├── JwtUtil.java
│   │   │   │   │   └── SecurityConfig.java
│   │   │   │   ├── controller/   # REST APIコントローラー
│   │   │   │   │   ├── AuthController.java
│   │   │   │   │   ├── UserController.java
│   │   │   │   │   ├── PartnerController.java
│   │   │   │   │   └── ProjectController.java
│   │   │   │   ├── dto/          # データ転送オブジェクト
│   │   │   │   │   ├── LoginRequest.java
│   │   │   │   │   ├── LoginResponse.java
│   │   │   │   │   ├── UserRequest.java
│   │   │   │   │   ├── UserResponse.java
│   │   │   │   │   └── ...
│   │   │   │   ├── entity/       # エンティティクラス
│   │   │   │   │   ├── User.java
│   │   │   │   │   ├── Partner.java
│   │   │   │   │   ├── Project.java
│   │   │   │   │   └── ProjectAssignment.java
│   │   │   │   ├── repository/   # JPA リポジトリ
│   │   │   │   │   ├── UserRepository.java
│   │   │   │   │   ├── PartnerRepository.java
│   │   │   │   │   ├── ProjectRepository.java
│   │   │   │   │   └── ...
│   │   │   │   ├── service/      # ビジネスロジック
│   │   │   │   │   ├── UserService.java
│   │   │   │   │   ├── PartnerService.java
│   │   │   │   │   ├── ProjectService.java
│   │   │   │   │   └── ...
│   │   │   │   └── util/         # ユーティリティ
│   │   │   └── resources/
│   │   │       └── application.yml
│   │   └── test/             # テストコード
│   ├── Dockerfile
│   └── pom.xml
│
├── frontend/             # React アプリケーション
│   ├── public/
│   ├── src/
│   │   ├── components/   # 再利用可能コンポーネント
│   │   │   ├── Navbar.js
│   │   │   ├── PrivateRoute.js
│   │   │   └── Spreadsheet.js
│   │   ├── context/      # コンテキスト
│   │   │   └── AuthContext.js
│   │   ├── pages/        # ページコンポーネント
│   │   │   ├── Login.js
│   │   │   ├── Dashboard.js
│   │   │   ├── Accounts.js
│   │   │   ├── Partners.js
│   │   │   ├── Projects.js
│   │   │   └── ProjectDetail.js
│   │   ├── services/     # API通信サービス
│   │   │   ├── api.js
│   │   │   ├── authService.js
│   │   │   ├── userService.js
│   │   │   ├── partnerService.js
│   │   │   └── projectService.js
│   │   ├── App.js
│   │   └── index.js
│   └── package.json
│
└── docker-compose.yml
```

---

### 3.2 重要なファイルの説明

#### Backend

**application.yml**:
```yaml
# アプリケーション設定ファイル
# プロファイル（dev, prod, test）ごとに設定を切り替え可能
```

**SecurityConfig.java**:
```java
// Spring Securityの設定
// JWT認証フィルター、CORS設定、アクセス制御ルールを定義
```

**JwtUtil.java**:
```java
// JWTトークンの生成・検証ロジック
// トークンの有効期限、署名キーなどを管理
```

#### Frontend

**api.js**:
```javascript
// Axiosインスタンスの設定
// リクエストインターセプター（JWTトークン付加）
// レスポンスインターセプター（エラーハンドリング）
```

**AuthContext.js**:
```javascript
// 認証状態の管理
// ログイン・ログアウト機能
// ロール判定（isAdmin, isRep, isSystem）
```

---

## 4. 開発ワークフロー

### 4.1 Gitワークフロー

#### ブランチ戦略

```
main (本番環境)
  └── develop (開発環境)
       ├── feature/user-management
       ├── feature/partner-csv
       └── bugfix/login-error
```

**推奨ワークフロー**:

1. **新機能開発**:
```bash
# developブランチから作業ブランチを作成
git checkout develop
git pull origin develop
git checkout -b feature/new-feature

# 作業
# ...

# コミット
git add .
git commit -m "feat: 新機能の説明"

# プッシュ
git push origin feature/new-feature

# Pull Requestを作成（GitHub上）
```

2. **バグ修正**:
```bash
git checkout develop
git checkout -b bugfix/fix-description

# 修正作業
git add .
git commit -m "fix: バグ修正の説明"
git push origin bugfix/fix-description
```

---

#### コミットメッセージ規約

**フォーマット**:
```
<type>: <subject>

<body>

<footer>
```

**Type**:
- `feat`: 新機能
- `fix`: バグ修正
- `docs`: ドキュメント変更
- `style`: コードフォーマット（機能変更なし）
- `refactor`: リファクタリング
- `test`: テスト追加・修正
- `chore`: ビルド・設定変更

**例**:
```bash
git commit -m "feat: パートナーCSVインポート機能を追加"
git commit -m "fix: ログイン時のトークン検証エラーを修正"
git commit -m "docs: README にデプロイ手順を追加"
```

---

### 4.2 開発サイクル

#### 1. 要件確認

- GitHub Issues または Jira で タスクを確認
- 不明点があれば質問

#### 2. 設計

- 必要に応じてER図・画面設計を更新
- API仕様を確認・更新

#### 3. 実装

**Backend**:
```bash
# Entity作成 → Repository → Service → Controller の順
cd backend/src/main/java/com/example/prmtool/

# Entity作成
vim entity/NewEntity.java

# Repository作成
vim repository/NewEntityRepository.java

# Service作成
vim service/NewEntityService.java

# Controller作成
vim controller/NewEntityController.java

# DTO作成
vim dto/NewEntityRequest.java
vim dto/NewEntityResponse.java
```

**Frontend**:
```bash
cd frontend/src/

# Serviceの作成（API通信）
vim services/newEntityService.js

# Pageコンポーネントの作成
vim pages/NewEntityPage.js
vim pages/NewEntityPage.css

# App.jsにルートを追加
vim App.js
```

#### 4. テスト

```bash
# Backendテスト
cd backend
mvn test

# Frontendテスト（将来実装）
cd frontend
npm test
```

#### 5. 動作確認

- ブラウザで実際に操作して確認
- 各種ロール（ADMIN, REP）で動作確認
- エラーハンドリングの確認

#### 6. コミット・プッシュ

```bash
git add .
git commit -m "feat: 新機能の説明"
git push origin feature/branch-name
```

---

## 5. コーディング規約

### 5.1 Backend (Java)

#### 命名規則

```java
// クラス名: PascalCase
public class UserService { }

// メソッド名: camelCase
public UserResponse getUserById(UUID id) { }

// 変数名: camelCase
private String loginId;

// 定数: UPPER_SNAKE_CASE
private static final int MAX_RETRY_COUNT = 3;

// パッケージ名: 小文字
package com.example.prmtool.service;
```

#### コメント規約

```java
/**
 * ユーザー情報を取得する
 * 
 * @param id ユーザーID
 * @return ユーザーレスポンス
 * @throws RuntimeException ユーザーが見つからない場合
 */
public UserResponse getUserById(UUID id) {
    // ユーザーを検索
    User user = userRepository.findById(id)
        .orElseThrow(() -> new RuntimeException("ユーザーが見つかりません"));
    
    // DTOに変換して返却
    return mapToResponse(user);
}
```

**重要**:
- すべてのpublicメソッドにJavadocコメントを記載
- 複雑なロジックには処理内容を説明するコメントを追加
- 「何をしているか」ではなく「なぜそうしているか」を書く

---

### 5.2 Frontend (JavaScript/React)

#### 命名規則

```javascript
// コンポーネント名: PascalCase
const UserList = () => { }

// 関数名: camelCase
const fetchUsers = async () => { }

// 変数名: camelCase
const userName = 'John';

// 定数: UPPER_SNAKE_CASE
const API_BASE_URL = 'http://localhost:8080';

// ファイル名: PascalCase (コンポーネント), camelCase (サービス)
// UserList.js, userService.js
```

#### コメント規約

```javascript
/**
 * ユーザー一覧を取得する
 */
const fetchUsers = async () => {
  try {
    // API呼び出し
    const data = await userService.getAll();
    
    // Stateを更新
    setUsers(data);
  } catch (error) {
    // エラーメッセージを表示
    setError('ユーザー一覧の取得に失敗しました');
  }
};
```

---

### 5.3 CSS

```css
/* BEM命名規則を推奨 */
/* Block__Element--Modifier */

/* Block */
.user-list { }

/* Element */
.user-list__item { }
.user-list__header { }

/* Modifier */
.user-list__item--active { }
.user-list__item--disabled { }
```

---

## 6. デバッグ方法

### 6.1 Backend デバッグ

#### ログ出力

```java
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class UserService {
    private static final Logger log = LoggerFactory.getLogger(UserService.class);
    
    public UserResponse getUserById(UUID id) {
        log.debug("getUserById called with id: {}", id);
        
        User user = userRepository.findById(id)
            .orElseThrow(() -> {
                log.error("User not found: {}", id);
                return new RuntimeException("ユーザーが見つかりません");
            });
        
        log.info("User found: {}", user.getLoginId());
        return mapToResponse(user);
    }
}
```

**ログレベル**:
- `ERROR`: エラー発生時
- `WARN`: 警告
- `INFO`: 重要な情報（本番環境でも出力）
- `DEBUG`: デバッグ情報（開発環境のみ）
- `TRACE`: 詳細なトレース情報

---

#### IntelliJ IDEA でデバッグ

1. ブレークポイントを設定（行番号の左をクリック）
2. 「Debug」ボタンでアプリケーションを起動
3. ブレークポイントで停止
4. 変数の値を確認、ステップ実行

**便利なショートカット**:
- `F8`: ステップオーバー（次の行へ）
- `F7`: ステップイン（メソッド内部へ）
- `Shift+F8`: ステップアウト（メソッドから出る）
- `F9`: 次のブレークポイントまで実行

---

### 6.2 Frontend デバッグ

#### Console.log

```javascript
const fetchUsers = async () => {
  console.log('fetchUsers called');
  
  try {
    const data = await userService.getAll();
    console.log('Users fetched:', data);
    setUsers(data);
  } catch (error) {
    console.error('Error fetching users:', error);
    setError('ユーザー一覧の取得に失敗しました');
  }
};
```

---

#### ブラウザ開発者ツール

**Chrome DevTools**:
1. `F12` または `Ctrl+Shift+I` で開く
2. **Console**: ログ確認
3. **Network**: API通信確認
4. **Sources**: ブレークポイント設定、ステップ実行
5. **Application > Local Storage**: JWTトークン確認

**デバッグ手順**:
1. `Sources` タブを開く
2. ファイルを開いてブレークポイントを設定
3. ページをリロードまたは操作を実行
4. ブレークポイントで停止
5. 変数の値を確認、ステップ実行

---

#### React Developer Tools

1. [React Developer Tools](https://chrome.google.com/webstore/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi) をインストール
2. 開発者ツールに `Components` タブと `Profiler` タブが追加される
3. **Components**: コンポーネントツリーとStateを確認
4. **Profiler**: パフォーマンス計測

---

### 6.3 データベースデバッグ

#### pgAdmin でSQL実行

1. pgAdmin を開く
2. `PRM Tool Local` サーバーに接続
3. `prmdb` を選択
4. 「Query Tool」を開く
5. SQLを実行

```sql
-- ユーザー一覧
SELECT * FROM users;

-- パートナー一覧
SELECT * FROM partners;

-- 案件と関連データ
SELECT 
    p.name AS project_name,
    pt.name AS partner_name,
    u.name AS owner_name,
    p.status
FROM projects p
JOIN partners pt ON p.partner_id = pt.id
JOIN users u ON p.owner_id = u.id;

-- 案件担当者
SELECT 
    p.name AS project_name,
    u.name AS assigned_user
FROM projects p
JOIN project_assignments pa ON p.id = pa.project_id
JOIN users u ON pa.user_id = u.id;
```

---

## 7. テスト

### 7.1 Backend テスト

#### Unit Test (JUnit)

```java
package com.example.prmtool.service;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
class UserServiceTest {

    @Autowired
    private UserService userService;

    @Test
    void testCreateUser() {
        // Given
        UserRequest request = new UserRequest();
        request.setName("Test User");
        request.setLoginId("testuser");
        request.setEmail("test@example.com");
        request.setRole(User.UserRole.REP);

        // When
        UserResponse response = userService.createUser(request);

        // Then
        assertNotNull(response);
        assertEquals("Test User", response.getName());
        assertEquals("testuser", response.getLoginId());
    }
}
```

**テスト実行**:
```bash
# 全テスト実行
mvn test

# 特定のテストクラスのみ実行
mvn test -Dtest=UserServiceTest

# 特定のテストメソッドのみ実行
mvn test -Dtest=UserServiceTest#testCreateUser
```

---

### 7.2 API テスト (Postman)

#### コレクションの作成

1. Postman を起動
2. 新しいコレクション「PRM Tool API」を作成
3. リクエストを追加

**ログインリクエスト**:
```
POST http://localhost:8080/api/auth/login
Content-Type: application/json

{
  "loginId": "system",
  "password": "SystemPass123!"
}
```

**ユーザー一覧取得**:
```
GET http://localhost:8080/api/users
Authorization: Bearer <JWT_TOKEN>
```

#### 環境変数の設定

1. コレクション設定を開く
2. `Variables` タブで以下を設定:
   - `baseUrl`: `http://localhost:8080`
   - `token`: (ログイン後に設定)

3. リクエストで変数を使用:
```
GET {{baseUrl}}/api/users
Authorization: Bearer {{token}}
```

---

## 8. トラブルシューティング

### 8.1 Backend 関連

#### エラー: Port 8080 is already in use

**原因**: 既に8080ポートが使用されている

**解決策**:
```bash
# Windowsの場合
netstat -ano | findstr :8080
taskkill /PID <PID> /F

# macOS/Linuxの場合
lsof -i :8080
kill -9 <PID>

# または別のポートを使用
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8081
```

---

#### エラー: Cannot connect to database

**原因**: PostgreSQLコンテナが起動していない

**解決策**:
```bash
# コンテナの状態確認
docker ps

# コンテナが停止している場合
docker-compose up -d

# ログ確認
docker logs prmtool-postgres
```

---

#### エラー: Schema validation failed

**原因**: エンティティとDBスキーマの不一致

**解決策**:
```bash
# 開発環境の場合、テーブルを再作成
# application.yml で以下を設定
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

# または手動でテーブル削除
docker exec -it prmtool-postgres psql -U prmuser -d prmdb
DROP TABLE users CASCADE;
DROP TABLE partners CASCADE;
DROP TABLE projects CASCADE;
DROP TABLE project_assignments CASCADE;
\q

# アプリケーション再起動
mvn spring-boot:run
```

---

### 8.2 Frontend 関連

#### エラー: npm install failed

**原因**: node_modules が破損している

**解決策**:
```bash
# node_modules と package-lock.json を削除
rm -rf node_modules package-lock.json

# 再インストール
npm install
```

---

#### エラー: CORS error

**原因**: Backend のCORS設定が正しくない

**解決策**:
1. `backend/src/main/java/com/example/prmtool/config/CorsConfig.java` を確認
2. `http://localhost:3000` が許可されているか確認

```java
configuration.setAllowedOrigins(Arrays.asList(
    "http://localhost:3000",  // これが必要
    "https://prmtool-frontend.onrender.com"
));
```

---

#### エラー: 401 Unauthorized

**原因**: JWTトークンが無効または期限切れ

**解決策**:
1. ローカルストレージを確認:
   - ブラウザ開発者ツール → Application → Local Storage
   - `token` キーがあるか確認
2. トークンが期限切れの場合、再ログイン
3. トークンが無い場合、ログイン処理を確認

---

## 9. よくある質問

### Q1. データベースをリセットしたい

**A**: 以下のコマンドでコンテナを削除して再作成

```bash
# コンテナを停止・削除
docker-compose down

# ボリュームも削除（データが完全に削除される）
docker-compose down -v

# 再作成
docker-compose up -d

# アプリケーション再起動（テーブル再作成）
cd backend
mvn spring-boot:run
```

---

### Q2. 初期管理者のパスワードを忘れた

**A**: データベースから直接削除して再作成

```bash
# PostgreSQLに接続
docker exec -it prmtool-postgres psql -U prmuser -d prmdb

# systemユーザーを削除
DELETE FROM users WHERE login_id = 'system';
\q

# アプリケーション再起動（自動的に再作成される）
mvn spring-boot:run
```

---

### Q3. Mavenの依存関係がダウンロードできない

**A**: キャッシュをクリアして再試行

```bash
# Maven キャッシュをクリア
mvn dependency:purge-local-repository

# 再インストール
mvn clean install -U
```

---

### Q4. Reactアプリが更新されない

**A**: ビルドキャッシュをクリア

```bash
# node_modules を削除
rm -rf node_modules

# npm キャッシュをクリア
npm cache clean --force

# 再インストール
npm install

# 再起動
npm start
```

---

### Q5. Docker Desktop が起動しない

**A**: WSL2 の確認（Windows）

```bash
# PowerShellで実行
wsl --list --verbose

# WSL2が有効でない場合
wsl --set-default-version 2
```

---

## 📚 参考リンク

- [Spring Boot公式ドキュメント](https://spring.io/projects/spring-boot)
- [React公式ドキュメント](https://react.dev)
- [PostgreSQL公式ドキュメント](https://www.postgresql.org/docs/)
- [Docker公式ドキュメント](https://docs.docker.com/)
- [Maven公式ドキュメント](https://maven.apache.org/guides/)
- [JWT公式サイト](https://jwt.io/)

---

## 🆘 サポート

問題が発生した場合:

1. このガイドの「トラブルシューティング」を確認
2. GitHub Issues で既存の問題を検索
3. 新しいIssueを作成（エラーログとスクリーンショットを添付）

---

**Happy Coding! 🚀**

**Last Updated**: 2026-01-20
